# Better Auth
# Generate a secret with: openssl rand -base64 32
BETTER_AUTH_SECRET=some_secrets_are_nicer_kept_that_way
BETTER_AUTH_URL=http://localhost:3000

# Database (uncomment when Drizzle is setup)
# DATABASE_URL=postgresql://user:password@localhost:5432/buddy_on_call
